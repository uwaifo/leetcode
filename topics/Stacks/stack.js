//palindrom

let letters = [];
let word = "racecar";
let reverse_word = "";
for (let i = 0; i < word.length; i++) {
  letters.push(word[i]);
  //console.log(letters);
}

for (let i = 0; i < word.length; i++) {
  reverse_word .pop(word[i]);
  console.log(letters);
}
